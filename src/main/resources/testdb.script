CREATE SCHEMA PUBLIC AUTHORIZATION DBA
CREATE MEMORY TABLE H_USER(ID INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, NAME VARCHAR(80) NOT NULL)
CREATE MEMORY TABLE H_OFFICE_USER(USER_ID INTEGER PRIMARY KEY, OFFICE VARCHAR(80) NOT NULL)
CREATE MEMORY TABLE H_BOAT(ID INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, NAME VARCHAR(80) NOT NULL, BOAT_TYPE VARCHAR(80) NOT NULL)
CREATE MEMORY TABLE H_PLANE(ID INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, PLANE_NAME VARCHAR(80) NOT NULL)
CREATE USER SA PASSWORD ""
GRANT DBA TO SA
SET WRITE_DELAY 10